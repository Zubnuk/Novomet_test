<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-header bg-warning text-dark">
                <h5 class="mb-0">Редактировать водителя</h5>
            </div>
            <div class="card-body">
                <form action="/handlers/update/update_driver.php" method="post">
                    <input type="hidden" name="driver_id" value="{{driver_id}}">

                    <div class="mb-3">
                        <label for="full_name" class="form-label">ФИО Водителя</label>
                        <input type="text" 
                               id="full_name" 
                               name="full_name" 
                               class="form-control form-control-lg" 
                               value="{{full_name}}" 
                               required 
                               minlength="5">
                        <div class="form-text">Введите полное имя (минимум 5 символов).</div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-start mt-4">
                            <a href="/lists/drivers.php" class="btn btn-outline-secondary px-4">
                            <i class="bi bi-arrow-left"></i> Назад к списку
                             </a>
                        <button type="submit" class="btn btn-primary px-5">Сохранить изменения</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>