<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card shadow-sm border-primary">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">Редактировать маршрут</h5>
            </div>
            <div class="card-body p-4">
                <form action="/handlers/update/update_route.php" method="post">
                    <input type="hidden" name="route_id" value="{{route_id}}">

                    <div class="mb-4">
                        <label for="route_number" class="form-label fw-bold">Номер маршрута</label>
                        <input type="text" 
                               id="route_number" 
                               name="route_number" 
                               class="form-control form-control-lg border-primary" 
                               value="{{route_number}}" 
                               placeholder="Например: 10А или 101"
                               required>
                        <div class="form-text mt-2">
                            Этот номер будет отображаться в списке транспорта и на остановках.
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-start mt-4">
                            <a href="/lists/routes.php" class="btn btn-outline-secondary px-4">
                            <i class="bi bi-arrow-left"></i> Назад к списку
                             </a>
                        <button type="submit" class="btn btn-primary px-5">Сохранить изменения</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>