<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card shadow-sm border-secondary">
            <div class="card-header bg-secondary text-white">
                <h5 class="mb-0">Редактировать тип транспорта</h5>
            </div>
            <div class="card-body p-4">
                <form action="/handlers/update/update_type.php" method="post">
                    <input type="hidden" name="type_id" value="{{type_id}}">

                    <div class="mb-4">
                        <label for="type_name" class="form-label fw-bold">Название категории</label>
                        <input type="text" 
                               id="type_name" 
                               name="name" 
                               class="form-control form-control-lg" 
                               value="{{name}}" 
                               placeholder="Например: Трамвай"
                               required>
                        <div class="form-text mt-2">
                            Изменение названия отразится на всех транспортных средствах этой категории.
                        </div>
                    </div>

                    <div class="d-flex justify-content-between align-items-center">
                        <a href="/list.php" class="btn btn-link text-muted p-0 text-decoration-none">
                            ← Назад к списку
                        </a>
                        <button type="submit" class="btn btn-secondary px-4 shadow-sm">
                            Обновить категорию
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>